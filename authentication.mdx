---
title: Authentication
description: "Getting started using the API"
---

Every request to the API must be authenticated. This is done by passing a bearer token in the `Authorization` header.

The token can be found on the [Flash Proxy Reseller Dashboard](https://www.flashproxy.io/dashboard/reseller).
